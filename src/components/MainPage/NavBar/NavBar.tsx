import React from 'react';
import {NavBarStyled} from "./NavBarStyled";

const NavBar = () => {
    return (
        <NavBarStyled>
            NavBar
        </NavBarStyled>
    );
};

export default NavBar;